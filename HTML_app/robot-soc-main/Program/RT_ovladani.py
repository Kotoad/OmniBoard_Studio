import logging
import os
import subprocess
logging.basicConfig(level=logging.DEBUG)
def stop(command, smer, rychlost):
    if command == 'stop':
        #logging.info("Stop command received")
        #logging.info(f"Směr: {smer}")
        #logging.info(f"Rychlost: {rychlost}")
        pass

def vpred(command, smer, rychlost):
    if command == 'start':
        # subprocess.run("cls" if os.name == "nt" else "clear", shell=True)
        #logging.info("Start command received")
        #logging.info(f"Směr: {smer}")
        #logging.info(f"Rychlost: {rychlost}")
        pass

def vzad(command, smer, rychlost):
    if command == 'start':
        # subprocess.run("cls" if os.name == "nt" else "clear", shell=True)
        #logging.info("Start command received")
        #logging.info(f"Směr: {smer}")
        #logging.info(f"Rychlost: {rychlost}")
        pass

def v_pravo(command, smer, rychlost):
    if command == 'start':
        # subprocess.run("cls" if os.name == "nt" else "clear", shell=True)
        #logging.info("Start command received")
        #logging.info(f"Směr: {smer}")
        #logging.info(f"Rychlost: {rychlost}")
        pass

def v_levo(command, smer, rychlost):
    if command == 'start':
        # subprocess.run("cls" if os.name == "nt" else "clear", shell=True)
        #logging.info("Start command received")
        #logging.info(f"Směr: {smer}")
        #logging.info(f"Rychlost: {rychlost}")
        pass